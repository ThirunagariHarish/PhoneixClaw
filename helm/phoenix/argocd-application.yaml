apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: phoenix
  namespace: argocd
  labels:
    app.kubernetes.io/name: phoenix
    app.kubernetes.io/part-of: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/ThirunagariHarish/PhoneixClaw.git
    targetRevision: main
    path: helm/phoenix
    helm:
      # Default to local images already imported into k3s containerd via
      # `docker save | k3s ctr images import`. Switch to values.prod.yaml once
      # the 14 service images are pushed to ghcr.io (see CD pipeline).
      valueFiles:
      - values.local.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: phoenix
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    retry:
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
      limit: 5
    syncOptions:
    - CreateNamespace=true
    - ApplyOutOfSyncOnly=true
    - ServerSideApply=true
  revisionHistoryLimit: 10
