apiVersion: apps/v1
kind: Deployment
metadata:
  name: phoenix-discord-ingestion
  labels:
    {{- include "phoenix.labels" . | nindent 4 }}
    app.kubernetes.io/component: phoenix-discord-ingestion
spec:
  replicas: 1
  strategy:
    type: Recreate
    rollingUpdate: null
  selector:
    matchLabels:
      {{- include "phoenix.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: phoenix-discord-ingestion
  template:
    metadata:
      labels:
        {{- include "phoenix.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: phoenix-discord-ingestion
    spec:
      containers:
      - name: discord-ingestion
        image: {{ .Values.image.repository }}/phoenix-discord-ingestion:{{ .Values.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 8060
          name: http
        envFrom:
        - configMapRef:
            name: phoenix-config
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: phoenix-secrets
              key: POSTGRES_PASSWORD
        - name: DATABASE_URL
          value: "postgresql+asyncpg://{{ .Values.postgres.user }}:$(POSTGRES_PASSWORD)@postgres:5432/{{ .Values.postgres.database }}"
        - name: CREDENTIAL_ENCRYPTION_KEY
          valueFrom:
            secretKeyRef:
              name: phoenix-secrets
              key: CREDENTIAL_ENCRYPTION_KEY
        - name: FERNET_KEY
          valueFrom:
            secretKeyRef:
              name: phoenix-secrets
              key: CREDENTIAL_ENCRYPTION_KEY
        livenessProbe:
          httpGet:
            path: /health
            port: 8060
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8060
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            memory: 256Mi
          limits:
            memory: {{ .Values.resources.discordIngestion.memory }}
